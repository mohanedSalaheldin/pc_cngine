import 'package:flutter/material.dart';

class AskDocScreen extends StatelessWidget {
  const AskDocScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('AskDocScreen'),
      ),
    );
  }
}